# Privacy Policy

This plugin sends only the URL and selected extraction options to `https://markdown.new/` to convert web content into Markdown.

The plugin itself does not store credentials, does not require API keys, and does not persist request data.

Please review the markdown.new service terms for their own data-handling policy.
